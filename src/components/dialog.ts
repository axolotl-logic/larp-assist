export default {};
/*export const dialogEmits = [ 'ok', 'hide' ]

export const dialogMethods = (save) => ({
    show () {
      this.$refs.dialog.show()
    },

    hide () {
      this.$refs.dialog.hide()
    },

    onDialogHide () {
      this.$emit('hide')
    },
})*/
